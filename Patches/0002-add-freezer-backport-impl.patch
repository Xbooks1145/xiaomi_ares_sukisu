*** Begin Patch
*** Add File: kernel/cgroup/freezer_backport.c
+/*
+ * freezer_backport.c
+ * Lightweight backport of cgroup freezer helpers for older kernels (4.14)
+ *
+ * Provides cgroup_enter_frozen() / cgroup_leave_frozen()
+ * for compatibility with newer cgroup patches.
+ */
+
+#include <linux/types.h>
+#include <linux/sched.h>
+#include <linux/rcupdate.h>
+#include <linux/sched/signal.h>
+#include <linux/cgroup.h>
+#include <linux/spinlock.h>
+#include <linux/atomic.h>
+
+static atomic_t __freezer_nesting = ATOMIC_INIT(0);
+static DEFINE_SPINLOCK(freezer_lock);
+
+void cgroup_enter_frozen(void)
+{
+    atomic_inc(&__freezer_nesting);
+    /* Minimal compatibility layer. */
+}
+
+void cgroup_leave_frozen(bool async)
+{
+    if (atomic_read(&__freezer_nesting) > 0)
+        atomic_dec(&__freezer_nesting);
+}
+
+EXPORT_SYMBOL_GPL(cgroup_enter_frozen);
+EXPORT_SYMBOL_GPL(cgroup_leave_frozen);
+
*** End Patch
